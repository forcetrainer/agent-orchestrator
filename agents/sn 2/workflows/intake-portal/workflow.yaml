# Portal/UI Customization Intake Configuration
name: "intake-portal"
description: "Initial requirements gathering for ServiceNow portal and UI customization requests. Quick user-facing session (3-5 minutes) to capture problem statement, business impact, and initial requirements."
author: "Alex the Facilitator"

date: system-generated

# Module path and component files
installed_path: "{project-root}/bmad/sn/workflows/intake-portal"
template: "{project-root}/bmad/sn/templates/initial-requirements.md"
instructions: "{installed_path}/instructions.md"

# Category specific
category: "Portal/UI Customization"
request_type: "portal"

# Project identification (gathered during workflow)
project_slug: ""

# Output configuration - New structure
default_output_file: "{{project_slug}}/1-intake/initial-{{project_slug}}-{{date}}.md"